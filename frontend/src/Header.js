import React from 'react';

export default function Header({children}){
    return (
        <header> {children}</header>

    );
}
//export default Header; pode ser exportado no final do arquivo ou na declaração da função como acima...
